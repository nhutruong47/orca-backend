# ===== Database (PostgreSQL trên Neon.tech) =====
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.use_nationalized_character_data=false

# ===== JWT =====
app.jwt.secret=${JWT_SECRET:bXlTdXBlclNlY3JldEtleUZvckpXVF9QbGVhc2VDaGFuZ2VJblByb2R1Y3Rpb25fQXRMZWFzdDI1NkJpdHMhIQ==}
app.jwt.expiration=86400000

# ===== Google OAuth2 =====
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

# ===== Mail / SMTP =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:truongthainhuu@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xugnybrzlbqleztr}
spring.mail.test-connection=false
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000

# ===== Cấu hình Mạng =====
server.port=${PORT:8080}

# ===== Frontend URL (cho OAuth2 redirect) =====
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# ===== AI Service =====
ai.service.api-key=${AI_SERVICE_API_KEY:AIzaSyD0siixycjzxopyh3-3kfsjhLCaQDiN9Lc}
